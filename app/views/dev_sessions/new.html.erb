<h2>Select a user to sign in</h2>
<% @users.each do |user| %>
  <div style="margin-bottom: 10px;">
    <%= button_to "#{user.name} (#{user.role})", dev_session_path, params: { user_id: user.id }, method: :post, style: "cursor: pointer; padding: 8px 16px;" %>
  </div>
<% end %>
